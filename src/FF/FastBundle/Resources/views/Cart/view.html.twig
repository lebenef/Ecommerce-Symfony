{% extends "FFFastBundle:Cart:menu.html.twig" %}
{% set total = 0 %}

{% block title %}
  Validation - {{ parent() }}
{% endblock %}

{% block body %}
<h2>Valider mon panier</h2>
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Produit</th>
                        <th>Quantité</th>
                        <th>Prix unitaire</th>
                        <th>Total </th>
                    </tr>
                </thead>
                <tbody>
                    {% if produits|length == 0 %}
                        <tr>
                            <td colspan="4"><center>Panier vide </center></td>
                        </tr>
                    {% endif %}
                    
                    {% for produit in produits %}
                    <tr>
                            <td>{{ produit.name }}</td>
                            <td>
                                {{ panier[produit.id] }}
                            </td>
                            <td>{{ produit.price }} €</td>
                            <td>{{ produit.price * panier[produit.id] }} €</td>
                        </form>
                    </tr>
                    {% set total = total + (produit.price * panier[produit.id]) %}
                    {% endfor %}
                </tbody>
            </table>
            
            <dl class="dl-horizontal pull-right">
                <dt>Total  :</dt><dd>{{ total }} €</dd>
                

            </dl>
            
            <div class="span3 pull-left">
                <dl class="pull-left">
                    <dt><h4>Adresse de livraison</h4></dt>
                    <dt>{{user.user.prenom }} {{ user.user.nom }}</dt>
                    <dt>{{ user.user.adresse }}</dt>
                    <dt>{{ user.user.codepostal }} {{ user.user.ville }} - {{ user.user.pays }}</dt>
                </dl>
            </div>
            

            
            <div class="clearfix"></div>
            <a href="{{ path('ff_fast_precommandec') }}" class="btn btn-success pull-right">Payer</a>
            <a href="{{ path('ff_fast_show') }}" class="btn btn-primary">Retour</a>
<br>
  <div id="paypal-button"></div>

  <script>
    paypal.Button.render({
        env: 'production', // Or 'sandbox'

        client: {
            sandbox:    'AfFVd4e-324ULFdh5xMR1Fq_w5uApNO9DYN6SunjFZFci_Pe_ZCkdj9496zKHnZWpV5uDhqcI0GASfK-',
            production: 'ATLIAcvS8sHzPLbU6ZY8fy3xVjuYxgPHqATzh689kmIYuNhKtuGbK3gd_orYlT24Iis2097eKWXV3eNd'
        },

      commit: true, // Show a 'Pay Now' button

      style: {
        color: 'gold',
        size: 'small'
      },

      payment: function(data, actions) {
        /* 
         * Set up the payment here 
         */
      },

      onAuthorize: function(data, actions) {
        /* 
         * Execute the payment here 
         */
      },

      onCancel: function(data, actions) {
        /* 
         * Buyer cancelled the payment 
         */
      },

      onError: function(err) {
        /* 
         * An error occurred during the transaction 
         */
      }
    }, '#paypal-button');
  </script>
  
        </div>

    </div>
</div>

{% endblock %}
