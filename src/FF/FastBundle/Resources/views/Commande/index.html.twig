{% extends "FFFastBundle::layout.html.twig" %}

{% block title %}
  Accueil - {{ parent() }}
{% endblock %}

{% block ffgammes_body %}

  <h2>Liste des Commmandes</h2>

  <ul>
    {% for commande in listCommandes %}
      <li>
        <a href="{{ path('ff_fast_viewco', {'id': commande.id}) }}">
          {{ commande.user.username }}
        </a>
      </li>
    {% endfor %}

  </ul>
  
<nav aria-label="Page navigation example">

    <ul class="pagination">
  {# On utilise la fonction range(a, b) qui crée un tableau de valeurs entre a et b #}
  {% for p in range(1, nbPages) %}
    <li class="page-item" {% if p == page %} class="page-item active" {% endif %}>
      <a class="page-link" href="{{ path('ff_fast_homeco', {'page': p}) }}">{{ p }}</a>
    </li>
  {% endfor %}
</ul>
</nav>

{% endblock %}