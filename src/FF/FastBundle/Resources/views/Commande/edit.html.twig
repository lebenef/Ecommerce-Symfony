

{% extends "FFCoreBundle:Core:menu.html.twig" %}
 {% block title %}Modification d'une commande  - {{ parent() }} {% endblock %}
{% block body %}
<main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4">
<div id="content" class="col-md-8">
<br>
  {% for key, messages in app.session.flashbag.all() %}
    {% for message in messages %}
      <div class="alert alert-{{key}}>
       <strong>succès!</strong> {{ message|trans({}, 'FOSUserBundle') }}
      </div>
    {% endfor %}
  {% endfor %}
  <h2>Editer une Commande </h2>




      {{ form_start(form, {'action': path('ff_fast_editco', {'idCommande': commande.id}), 'method': 'POST'}) }}

{{ form_widget(form.etat, {value : commande.etat }) }}


        {{ form_end(form) }}

  <p>
    <a href="{{ path('ff_fast_viewco', {'id': commande.id}) }}" class="btn btn-primary ">
    <i class="fa fa-arrow-left" aria-hidden="true"></i>
      Retour
    </a>
  </p>
</main>
{% endblock %}
