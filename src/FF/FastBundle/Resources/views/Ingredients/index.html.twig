
{% extends "FFFastBundle::layout.html.twig" %}

{% block title %}
  Accueil - {{ parent() }}
{% endblock %}

{% block ffgammes_body %}

  <h2>Mes Ingredients</h2>

  <ul>
    {% for ingredients in listIngredients %}
      <li>
        <a href="{{ path('ff_fast_viewi', {'id': ingredients.id}) }}">
          {{ ingredients.name }}
        </a>
      </li>
    {% endfor %}
  </ul>
  
<nav aria-label="Page navigation example">

    <ul class="pagination">
  {# On utilise la fonction range(a, b) qui crée un tableau de valeurs entre a et b #}
  {% for p in range(1, nbPages) %}
    <li class="page-item" {% if p == page %} class="page-item active" {% endif %}>
      <a class="page-link" href="{{ path('ff_fast_homei', {'page': p}) }}">{{ p }}</a>
    </li>
  {% endfor %}
</ul>
</nav>
  <p>
    <a href="{{ path('ff_fast_addi')}}" class="btn btn-success">
Ajouter un ingredient    </a>
  </p>
{% endblock %}